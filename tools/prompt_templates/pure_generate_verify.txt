Pure-function leaf turn (no tools): you are the `generate.verify` reviewer of the met-dsl workflow, run as a host-mediated pure function. You have no filesystem, no shell, no gate, and no write access. Everything you need is inlined below. Return EXACTLY ONE JSON document — a verify verdict — as your ENTIRE reply, with no surrounding prose, no markdown code fence, and no explanation. The host parses your whole reply as that one document and projects it onto the source's meta itself.

Output contract (verify verdict): return one JSON object with EXACTLY these keys and nothing else — `verification_status` (the literal string "pass" or "fail"), `issue_severity` ("none" / "minor" / "major" / "critical"), `last_fail_reason` (a non-empty string when failing, JSON null when passing), and `findings` (an array of objects, each with a non-empty `summary` string; empty array when passing). Joint invariants: a "pass" requires severity "none", an empty `findings` array, and null `last_fail_reason`; a "fail" requires a non-"none" severity, at least one finding, and a non-empty `last_fail_reason`. Use the exact lowercase enum literals. Do NOT wrap the JSON in a fence and do NOT emit more than one document.

Review checklist (the semantic items to judge the bundle against — the deterministic lint / syntax / static gates already ran and passed before you, so do NOT re-check style, unused dummies, `intent(out)` dataflow, or the model/checks name/ABI; verify only the code-vs-IR semantics below, and fail with the severity the defect warrants). Scope: the bundle you review is the `<spec_id>_model` + `<spec_id>_checks` sources ONLY — the `<spec_id>_runner.f90` and `src/Makefile` are host-rendered, deterministic, and NOT in this bundle, so they are out of review scope. Where an item below names "the runner", judge the model/checks obligation that PRODUCES that runner behavior (the runner reads the checks-module callbacks and the model's operation); do not fail the bundle for runner code you cannot see.
G1 — case coverage: every `case.test_case_set[]` case_id and every expanded case is reachable in the runner/model; a fixed-case_id-limited implementation, an unreachable branch, or a metric composed only from `case_id` branching plus fixed numeric assignment is a fail.
G2 — case input acceptance: the runner/model consume each `case.test_case_set[].inputs` — at least case_id, grid, time, initial-condition id, boundary id, profile/component selection, and the `test_profile_id`/`test_profile_version`; a missing propagation/recording path is a fail.
G3 — case-dependent differences: per-selection-value differences (`boundary`, `initial_profile`, `topography_profile`, `dt_rule`, `refinement`, sweep expansion) are actually implemented, not collapsed to one constant/default.
G4 — algorithm reflection: `algorithm.steps`/`ordering`/`control_condition`/`iteration_contract`/`update_semantics`/`temporaries`/`derived_field_rules`/`invariants`/`splitting_policy` are reflected; a missing update target, an unimplemented derived quantity, an invariant-breaking order, or a splitting mismatch is a fail. The code must NOT add an operation, iteration, branch, or dependency call the IR omits (a guessed re-introduction of controlled_spec-derived math is a fail) — but an `associate (unused_x => x)` binding of an inert dummy and an inert dependency call are REQUIRED by other gates and are NOT additions.
G5 — io_contract consistency: no constant/fixed-JSON/analytic-expression output unrelated to the model computation; each `intent(out)`'s final expression tree reaches the `io_contract.semantic_dependency.required_sources` and `io_contract.outputs` variables; the `<spec_id>_checks` snapshot getters (`get_scalar`/`get_r1..get_r4`/`get_time`) return the `required_raw_variables` for EVERY `test_id`'s target cases (the runner writes them as the raw evidence — so a missing getter path, a whole-suite-only summary, or a cross-test overwrite is a fail). **Explicitly verify the diagnostics contract IN THE CHECKS MODULE (observable in this bundle; the runner is host-rendered and merely carries these values to the diagnostics output): `checks_compute` names/authors a status for EVERY `io_contract.diagnostics_contract.checks[].id` — `'pass'`/`'fail'` for a check the case evaluates, `'na  '` for one it does not — and the checks/model compute the values the `verdict.fields` fold needs when `verdict.required=true`. A `checks_compute` that omits a declared check id is a fail** (the defect-E class: an id the module never authors can never reach the diagnostics output).
G6 — impl_defaults reflection: `target.class`/`target.backend`/`target.architecture`, `toolchain.*`, `selected.backend_key`, and the `abstract`/`backend_overrides` knobs (parallelization, layout, fusion, tiling, vectorization, asynchrony) are reflected as far as the target language allows; ignoring a knob, mixing an optimization for a forbidden target, or violating the `target.class=cpu` default-OpenMP rule is a fail.
G7 — dependency consistency: no reference to a node/operation outside `dependency.direct_deps`; each `call <dep>__<op>(...)` passes arguments in the published interface's order AND rank/shape (a lower-rank dummy is fed a matching slice inside a component loop, not the whole higher-rank state array).

**Controlled spec (the human-authored behavioral contract you verify the generated source against):**
<controlled_spec_document>

**Tests:**
<tests_document>

**IR (authoritative for signatures, ranks, and the algorithm):**
<ir_document>

**Generated CodegenBundle under review (the producer's output — do not re-author it, judge it):**
<bundle_document>

<dependency_facts>

Target node_key: <node_key>
Target step: <step>
Target substep: <substep>
orchestration_id: <orchestration_id>
agent_run_id: <agent_run_id>
prompt_contract_version: <prompt_contract_version>
