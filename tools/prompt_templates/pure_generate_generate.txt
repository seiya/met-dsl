Pure-function leaf turn (no tools): you are the `generate.generate` producer of the met-dsl workflow, run as a host-mediated pure function. You have no filesystem, no shell, no gate, and no write access. Everything you need is inlined below. Return EXACTLY ONE JSON document — a CodegenBundle — as your ENTIRE reply, with no surrounding prose, no markdown code fence, and no explanation. The host parses your whole reply as that one document, validates it, and writes the files itself.

Output contract (CodegenBundle, schema 1.0.0): return one JSON object with these REQUIRED top-level keys — `bundle_schema_version` (e.g. "1.0.0"); `optimization_unit` (`{"members": [<this node's node_key>]}`); `files` (an array where each entry has `logical_path` (the source filename, NOT `path`), `role` (one of `model`/`checks`/`helper`/`internal_module`), `language` ("fortran"), `member_node_key` (a member node_key, or null for a unit-shared private file), `content` (the FULL source text, NOT `text`), and `modules` (the Fortran module names that file defines)); `entrypoints` (an array where each entry has `symbol`, `kind` (`operation`/`checks_interface`), `node_key`, `defined_in`, and `module`); `target_lowering_plan` (`{"precision": {...}, "state_residency": ...}`); and `capability_requirements` (an array of `<capability>@<version>` strings your generated code relies on from the certified harness). The `capability_requirements` array must be NON-EMPTY and must name EXACTLY ONE execution-model capability — one of `sync_single_case` / `async_device_resident` / `batched_cases` — that the harness manifest below actually provides; today the only provided execution-model token is `sync_single_case@1`, so an empty array is REJECTED (`capability_requirements must be a non-empty array`) and so is a bundle carrying zero or two execution-model tokens (`must declare exactly one execution-model capability`) — additional NON-execution-model tokens are allowed only when a `state_bindings` entry captures through them (see below), so declare no capability your code does not use. The `target_lowering_plan.state_residency` value must be one of `host` / `device` / `distributed`, and any non-`host` residency additionally requires the harness capability that backs it (`device` needs `async_device_resident`, `distributed` needs `distributed_state`) — the manifest below provides neither, so write `"state_residency": "host"`. Operation-entrypoint cardinality is fixed by THIS node's KIND (the first `/`-segment of its node_key): a `problem` node publishes EXACTLY ONE operation entrypoint (its single integration update path), a `component` / `infrastructure` node publishes ONE OR MORE, and a `profile` publishes EXACTLY ZERO — its only entrypoints are the fixed `checks_interface` ABI names, and an operation entrypoint on a profile is REJECTED (`is a profile and publishes no operation`); when you must remove a wrongly-added operation entrypoint, remove ONLY that `entrypoints` entry, because every member (a `profile` included) must still keep its role `model` file in `files[]` — dropping the model file is a separate REJECTION (`optimization_unit member ... has no files[] entry of role model`). You MAY include the optional `state_bindings` — each entry a closed object carrying exactly `node_key`, `state_variable`, `storage_symbol`, `module`, `capture`, and `capability` — but ONLY to bind a `state_variable` the IR below declares in `algorithm.state_variables`. That declaration is the whole of your node's state: a name taken instead from the tests or from an `algorithm.steps` variable is an invented registration and is REJECTED (`state_bindings[0].state_variable 'u' is not an IR algorithm.state_variable (declared: [])`), and when the IR declares NO `algorithm.state_variables` you MUST omit `state_bindings` entirely — an EMPTY declared set accepts no binding, not every binding. When you do bind: `node_key` is THIS node's node_key (the unit's only member), and at most ONE binding may name any one `state_variable`; `module` must be a module defined by one of YOUR OWN `checks`-role files, since that is what publishes `storage_symbol` and the host renders `use <module>, only: <storage_symbol>` from it — your `<spec_id>_checks` module, NEVER `<spec_id>_model`; and `capture` must be `checks_getter` with a null `capability` UNLESS the harness manifest below lists a `state_registration@N` capability, that listing being the only thing that licenses `capture: "harness_registration"`. Declare a `state_registration@N` requirement in `capability_requirements` ONLY as part of such a binding, because the coupling runs BOTH ways: the binding must carry that exact token, and every token you declare must be carried by a binding that captures through it. A declared token that no binding captures is rejected, and so is any token the manifest does not list — `capability_requirements` states what your code USES, not what the harness offers. Author THIS node's physics and per-test logic from ITS IR and tests below. Use the field names `logical_path`/`content` — NOT `path`/`text`. Do NOT emit more than one document, do NOT wrap it in a fence, and do NOT return prose — if a requirement cannot be met, still return a well-formed bundle so the host can diagnose it.

Authoring rules (the deterministic gates your bundle must pass — the host runs them on your source AFTER you return, and you get no second look at them, so satisfy them on the first emission).
(1) Style lint (`Generate.lint`, fortitude): keep every source line UNDER 100 columns — 99 is the longest that passes, since S001 fires at exactly 100 (`line length of 100, exceeds maximum 100`); split with a trailing `&` continuation; import with an explicit `use <module>, only: <names>` list — never a bare `use` (C121); import from an intrinsic module with the `use, intrinsic :: iso_fortran_env, only: ...` form (C122); PORT011 rejects a number-literal kind on EITHER a real or an integer, so always declare with a named kind from `iso_fortran_env` — `real(dp)` after `use, intrinsic :: iso_fortran_env, only: dp => real64` (never `real(8)`), `integer(int64)` (never `integer(8)` or `integer(4)`) — and note the named kind comes FROM `iso_fortran_env`, so adding that import is itself subject to the C122 rule above: "fixing" PORT011 with a bare `use iso_fortran_env` is the classic cascade that earns a fresh C122 on the next attempt, so add the `intrinsic` modifier in the same edit; give every module a default accessibility statement (C131) — a bare `private` in the specification part — and then a `public :: <spec_id>__<op>` line for EVERY name the module must expose, because a bare `private` alone publishes nothing: it silences C131 and then breaks the next gate, where a consumer staged alongside you (your own checks module, a depending node) fails `Generate.syntax` with `Symbol '<name>' referenced at (1) not found in module '<spec_id>_model'`. State both halves, never just one; give every `select case` a `case default` arm (C011), even one that only guards an unreachable value; a `character(len=*)` dummy is legal ONLY with `intent(in)` — fortitude C072 rejects an assumed-length character dummy declared `intent(out)` or `intent(inout)`, so give every character `intent(out)`/`intent(inout)` dummy an EXPLICIT length: in `checks_compute(case_id, check_id, status)` the runner passes `case_id` and `check_id` as `intent(in)` (so `character(len=*)` is legal for those two), while `status` is the sole `intent(out)` character dummy and MUST carry an explicit `character(len=4)`, NEVER `character(len=*)`.
(2) The `implicit none` trap — this exact pair of mistakes cost a prior run its whole retry budget, so read it twice. The lint wants `implicit none` to carry the F2018 spec-list `implicit none (type, external)`, but the build runs `-std=f2008`, where that spec-list is a COMPILE ERROR. Do NOT "fix" one by introducing the other; do NOT oscillate between them. The ONE correct form is a plain `implicit none` with a suppression directive on its OWN LINE immediately above it: a line containing only `! allow(C003)`, then the next line `implicit none`. Putting the directive on the SAME line as `implicit none` is wrong twice over (the C003 finding survives AND you earn a FORT002 finding). Writing `implicit none (external)` or `implicit none (type, external)` is wrong (compile error under `-std=f2008`). Emit the two-line form everywhere and move on.
(3) Syntax gate (`Generate.syntax` — a real `gfortran -fsyntax-only -std=f2008` front-end run over your source staged with its dependency closure, so a `! allow(...)` directive CANNOT suppress anything here: fortitude has no unused-symbol rule and an unknown-rule allow self-rejects under FORT001/FORT002): identifiers must be 63 characters or shorter, and if `<spec_id>_model` or `<spec_id>__<op>` would itself exceed the limit, do NOT abbreviate it — the literal `spec_id` prefix is load-bearing (a depending node is required to `use <spec_id>_model`), so an overlong one is a spec-level problem to report, never a name for you to shorten; `stop` and `error stop` accept only a CONSTANT expression as their code/message — to report a runtime value, `print` it first and then `error stop` with a literal; the gate promotes `-Werror=unused-dummy-argument` and `-Werror=unused-variable`, which has two consequences — (a) a dummy an interface FIXES but your algorithm never consumes (an inert input, an ABI-fixed `name` / `case_id`) stays a live `intent(in)` dummy and is bound immediately after the declarations with the EMPTY block `associate (unused_<name> => <name>); end associate` — an arithmetic no-op such as `0*<name>` is forbidden as the binding, and it computes nothing on purpose, so put no work inside it; an unused LOCAL is deleted rather than bound — and (b) an `intent(out)` dummy your body never sets is this gate's error too (`declared INTENT(OUT) but was not set`), so assign every `intent(out)` you declare; Fortran identifiers are case-INSENSITIVE, so `Flux` and `flux` collide — when a local or dummy would collide with an IR-derived name, keep the IR name verbatim and rename YOUR side.
(4) Dependency-dataflow gate (`Generate.static` — it inspects `problem` nodes with dependencies, and every subroutine there that declares at least one `intent(out)` dummy): declare the incoming state `intent(in)` and the updated state (the IR's `io_contract.outputs` / `algorithm.required_update_paths` variables) `intent(out)`. What the gate checks is NOT merely that each `intent(out)` gets assigned — it is that the values a load-bearing `call <dep>__<op>(...)` RETURNS reach an `intent(out)` dummy through an ASSIGNMENT chain: capture the dependency's result in a call output argument, then ASSIGN it (directly or through a chain of assignments) into an `intent(out)` — `call <dep>__<op>(..., unext)` then `h_out = unext(...)`. The gate follows assignment RHS, NOT further calls, so routing a dependency result through another `call` (e.g. a `call copy(tmp, h_out)`) instead of assigning it does NOT count and FAILS. Assigning every `intent(out)` from your own local computation does not satisfy it either: a dependency result consumed only by an `intent(inout)` in-place update reaches no `intent(out)` and FAILS with `does not propagate dependency operation outputs to intent(out) dataflow`. The IR's `io_contract.semantic_dependency.required_sources` must likewise reach the `intent(out)` dataflow.
(5) Dependency calls: for each operation your node depends on, check the IR for a declared SINK for its result — a variable in any `algorithm.steps[]` entry (`inputs` or `outputs`), a `derived_field_rules` / `invariants` quantity, an `io_contract` output, or a `semantic_dependency.required_sources` variable. If a sink exists the call is load-bearing: bind its results to those declared variables and let them flow exactly as declared (rule 4 is how that is checked). IR variable names carry no provenance marker, so resolve the correspondence from the step semantics — and when the correspondence is UNCERTAIN, treat the call as load-bearing; that is the default, and inert is the narrow exception. If and ONLY IF no sink exists, the call is INERT: declare local work variables satisfying the published interface, assign every actual argument before the call (which is also what keeps the rule-4 gate silent — an actual assigned before the call is not counted as a dependency result), and let no result reach an `io_contract` output, a `diagnostics_contract` check input, or an `algorithm.invariants` quantity. Do NOT invent a purpose for an inert call — no probes, no thresholds, no validity flags, no branching on its result.

**Harness capabilities (host-provided — what the ONE certified harness your node depends on exposes to you; it is the only source of capabilities you may require, and no other harness's manifest is shown to you):**
<harness_capabilities>

**Target profile (host-resolved language / build-system / precision defaults for this node):**
<target_profile>

<dependency_facts>

**IR (the lowered intermediate representation — authoritative for signatures, ranks, and the algorithm you implement):**
<ir_document>

**Tests (the behaviors your generated source must satisfy):**
<tests_document>

**Host-rendered runner (the consumer of your checks module — the host authors and stages this file; you do NOT emit it):** your `<spec_id>_checks` module must define and publish ALL TEN of the fixed checks ABI names — `case_setup`, `case_run`, `get_time`, `get_scalar`, `get_r1`, `get_r2`, `get_r3`, `get_r4`, `checks_compute`, `metric_compute` — each as a SUBROUTINE, and all ten are required of EVERY node whatever subset this runner's `use <spec_id>_checks, only:` list happens to import (a deterministic gate checks the full ten, so publishing only the imported subset is rejected). Every ABI name is a subroutine by contract, and the runner reaches the ones it imports with a `call`, so a FUNCTION of any of these names can never satisfy it. For each name the runner DOES import, its `call` sites below are authoritative for that subroutine's dummy arguments — match them exactly. The remaining names are still required: define them as honest not-found stubs shaped like their imported siblings — `get_r<rank>(name, arr, found)` binds `name` with the rule-3 empty `associate`, does a zero-size `allocate(arr(0[, 0...]))`, and sets `found = .false.`; `get_scalar(name, val, found)` binds `name`, sets `val = 0.0_dp`, and sets `found = .false.`; and when this runner imports no `metric_compute` (a node declaring no metrics), stub it as `metric_compute(case_id, name, val, is_na, reason_na, found)` — bind `case_id` and `name`, set `val = 0.0_dp`, `is_na = .false.`, `reason_na = ''`, and `found = .false.`. Those stub bodies are compiled by `Generate.syntax` exactly like the rest, so rules 1 and 3 bind them in full: every one of the ten needs a `public ::` statement (a bare `private` publishes nothing on its own), every dummy the ABI fixes but the stub never consumes needs its `associate (unused_<name> => <name>); end associate` binding, and every `intent(out)` needs an assignment — a stub that just sets `found` earns `Unused dummy argument 'name'` and `Dummy argument 'val' ... declared INTENT(OUT) but was not set`. Read the runner for the ABI and the call sites ONLY, and copy NONE of its plumbing: it is the one file allowed to touch the harness, and a deterministic gate (`Generate.static`) rejects your sources for three things it does openly. (a) NEITHER your checks NOR your model source may `use` the harness module in any form (`use harness_...`, `use :: harness_...`, `use, non_intrinsic :: harness_...`) — the runner's `use harness_fortran_cpu_model, only: ...` block and its `harness_fortran_cpu__*` calls are the runner's job, never yours; your model publishes `<spec_id>__<op>` and your checks compute honest values, and the runner alone carries them to the harness. (b) Your checks module does NO file I/O — no `open(` — because the harness owns every write. (c) Your checks module must not contain the judge-artifact filenames `verdict.json`, `aggregate_verdict.json`, `summary.json`, or `trial_meta.json` ANYWHERE in it, including inside comments and string literals: the scan is a plain substring match over the lowered text, so quoting a filename the inlined tests document happens to name is enough to fail it. Say "the judgment output" in prose instead of naming the file.
<runner_document>

**Checks-module behavioral contract (semantics the certified harness folds and the deterministic gates enforce — author your `<spec_id>_checks` module to satisfy every clause; each is stated against the enforcing implementation, not paraphrased from prose).**
(A) Author an honest `status` for each id. The runner drives the check ids: it calls `checks_compute(case_id, check_id, status)` once per (case, IR-declared id), passing `check_id` as a literal taken VERBATIM from `io_contract.diagnostics_contract.checks[].id`. Your module authors NO id — renaming, dropping, or adding one is structurally impossible, and there is no `chk_ids`/`ncheck` array to size. Your one job is `status`, an `intent(out)` `character(len=4)`: for the check this (case, check_id) evaluates set it to `'pass'` or `'fail'` from this case's real computation, and for a check this case cannot evaluate (a cross-case reduction not yet computable, a check that does not apply) set `status = 'na  '` (width-4, right-padded — the only three values are `'pass'`, `'fail'`, `'na  '`). Branch on `check_id` — a `select case (check_id)` is the natural shape — and because `status` is `intent(out)` you MUST assign it on EVERY path (rule 3), including the `case default` arm rule 1 requires, which returns `'na  '`. A per-case predicate reads `checks.<id>.status` inside its target case's slice, and `post_execute` folds the same ids, so keep the status honest per case; do not pre-adjust for xfail (clause D).
(B) `metric_compute(case_id, name, val, is_na, reason_na, found)` semantics. The runner calls it once per (case, metric address) exactly as its inlined body shows. When this case produces the named metric, set `found=.true.` and `val` to the real computed value; when the value is honestly unavailable for this case set `found=.true., is_na=.true., reason_na='<short reason>'`; when this metric does not belong to this case at all set `found=.false.` (it is then omitted — a sparse metric is normal). A CROSS-CASE reduction (a convergence order, a symmetry residual) is returned as a per-case metric of the FIRST case where it becomes computable — earlier cases return `found=.false.` for it.
(C) Snapshot getters return shape-valid state always. `get_scalar`/`get_r1`/`get_r2`/`get_r3`/`get_r4` return the state this case computed, in the shape the IR `raw_requirements`/snapshot schema and the runner's calls fix. A case whose `case_setup` returned `ok=.false.` (a rejected guard / xfail case) STILL returns a shape-valid, allocated placeholder (e.g. zeros of the correct shape) for every snapshot variable it owns — never an unallocated array. A run whose snapshots are uniformly zero across every case is rejected by `post_execute` (`trivial placeholder detected`), so the accepted cases must return the values the run actually computed.
(D) Checks are honest, never pre-judged. `checks_compute` reports `'fail'` for a failing check even in an xfail case; the xfail exclusion and the top-level verdict fold are the harness `__write_diagnostics`'s job. Pre-adjusting here double-counts and breaks the guard-passes-at-top-level rule.
(E) Accumulator ordering. The runner delivers `case_id`s in SORTED order, so a module-level cross-case accumulator may depend only on cases whose `case_id` sorts before the emitting case's (`test_case_set` declaration order is NOT the run order). Key any cross-case accumulation by `case_id` in module-level state.
(F) `case_run(case_id, steps, cells_updated, ok)` counters are the real work done (a non-time-stepping component uses `steps=1`, `cells_updated` = cells touched); they are summed into `perf.json`.

<exemplar>

Target node_key: <node_key>
Target step: <step>
Target substep: <substep>
orchestration_id: <orchestration_id>
agent_run_id: <agent_run_id>
prompt_contract_version: <prompt_contract_version>
